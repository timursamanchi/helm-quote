apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quote-app.fullname" . }}-test-connection"
  labels:
    {{- include "quote-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quote-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
